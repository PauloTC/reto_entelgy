import styles from '../../src/styles/Countries.module.css'

export default function Modal(continent) {

  // const [ modal , setModal] = useState(null)

  return (
    <div>
      <h1>Este pais pertenece a: { continent } </h1>
    </div>
  )
}